import type { Gender, Plurality } from "./types";

export const defaultPlurality: Plurality = 'other';

export const defaultGender: Gender = 'n';

export const genderPriority: Gender[] = ['n', 'm', 'f'];